"""展示层契约：确定性图表规范（运营工作流计划 Task 8）。

本包只做一件事：把**已经落库的数据集**换成一份可渲染的声明。它不读库、不算钱、
不生成任何可执行代码，也不复制行数据：

- 数字的唯一来源是 `commerce/`（确定性聚合）与落库的 Artifact；
- 这里产出的 `ChartSpec` 只回答「哪份数据集的哪一列、按什么口径、画成什么图」；
- 前端按声明渲染，不许自行重算财务口径（spec §8）。

分层与 `commerce/__init__.py` 同一理由：不在包级重导出符号，避免
`runtime.models` ← `presentation.charts` ← `runtime.artifacts` 之间出现回边。
"""
