# approved 查询学习记忆本地开发例外

日期：2026-09-15  
状态：**仅授权本地开发；不构成发布门禁通过**

## 决定

项目所有者在受控 SQL 探索 Task 1–6 已完成本地实现、独立审查、父级回归和日期化验收后，明确选择“授权本地开发”，允许在 Task 11 统一发布门禁第 4–7 项尚未完成时，本地实施 `2026-09-14-approved-query-memory.md` 的 Task 1–6。

该决定只解除 approved 查询学习记忆的本地实施限制，不修改总设计中的生产发布标准，也不把任何未执行验证记为通过。

## 允许范围

- 仅实施 approved 查询学习记忆计划 Task 1–6；
- `APPROVED_QUERY_MEMORY_ENABLED=false` 保持默认关闭；
- 只使用本机 `*_test` 数据库、stub 模型和离线测试；
- 允许编写 `021_approved_query_memory.sql`，并仅在本机 `*_test` 数据库顺序执行和幂等重放；
- 允许在本机测试库验证 `bi_approver`、`bi_app` 与 reporting view 的最小权限边界；
- 允许实现人工审核 API 与本地审核面板，但不得在生产或预发布启用；
- 每个计划 Task 继续执行原生 high 子代理、独立审查、父级验证和单独提交；
- feature gate 关闭时，现有 Tool、数据库读取和用户可见行为必须保持不变。

## 仍然禁止

- 在生产或预发布流量中启用 approved 查询学习记忆；
- 真实 provider smoke、26 题 live 验收或真实模型记忆注入；
- 生产/预发布数据库访问、迁移、来源同步、回填、对账、部署、OIDC、SSE、计划任务、备份或恢复操作；
- 读取、保存或传播原始聊天、真实实体名称/主键、SQL、查询结果、DSN、密钥或错误堆栈；
- 让模型、成功运行、点赞或用户纠错自动创建、批准、撤销或替换长期记忆；
- 保存目标价、阈值、预算、日期区间或店铺选择等一次性业务值；
- PDD 连接器、来源、支付能力、凭据或 onboarding；
- 在本例外下开始隔离分析 Agent 或持续库存通知。

## 未完成门禁

`2026-09-14-task-11-release-acceptance.md` 记录的统一发布门禁第 4–7 项仍为 open：

1. 获准真实 provider smoke 与 26 题 live；
2. 目标环境迁移、来源及能力核对；
3. 可信身份、Origin、SSE、同步、重核、脱敏日志、备份和真实恢复；
4. 一店一周试用与问题归档。

本地契约、权限、检索、审核 UI 和离线验收通过不得改变这些状态。任何生产启用或后续子项目的门禁例外都需要新的、明确的所有者决定。
