# 2026-09-23 快麦真实来源 / 本机测试库限定窗口验证

范围：20 家已授权店（不含排除平台），北京时间 `[2026-09-17 00:00, 2026-09-18 00:00)`。真实快麦 API 只读；写入仅允许本机 `bi_agent_test`。逐店串行，最多 12 次 `KuaimaiClient.call`/店（客户端单次调用仍可能内部重试，不等同 HTTP 请求总数），有限超时，遇失败停止或不发布该窗口。未写生产数据库；未覆盖最近半个月。

| 步骤 | 结果 | 证据 |
| --- | --- | --- |
| 只读探测 | 20/20 成功，订单 523、售后发生 33；合计 60 次客户端调用 | 后台任务 `b6f80b614` |
| 订单 replay | 20/20 成功，写入/处理 523，阻止已核验支付降级 0；测试库有 20 家对应批次 | `bb5b26c0e` |
| 售后发生 replay | 20/20 成功，写入/处理 33；测试库有 20 家对应批次 | `b60603e4a` |
| 指定日重核 | 20/20 成功；订单批次处理 523，售后发生批次**更新数** 0（不代表 API 返回 0），该日付款订单的商业单 cohort 528。没有执行跨出指定日的全历史未匹配订单补拉 | `b3ba2fd52` |
| 质量凭证 | 订单、售后发生各 20 家 `passed`，检查时间在重核后刷新；退款 cohort 20 家该日覆盖，但质量仍为 `unknown`，不得称为核验通过 | 本机测试库 `reporting.v_coverage` |
| 真实 DeepSeek 查询 | “近半个月销售前十，缺口则前移”：`09-08→09-23` 为 `missing_data`，授权前移到 `09-03→09-18` 返回 `ok`；4 店 `certified`、4 店 `observable_sample` 各出 Top 10，另 12 店因 `coverage_time_basis_unverified` 被排除。回答陈述实际运行窗口，未声称未执行的尝试 | `b71cc923f`，仅本机测试库 |
| 回归 | 后端 1698 passed / 2524 subtests，3 warnings；离线验收 26/26；前后端健康端点各 200 | `b4716e289`、`bf05390e8` |

首次重核脚本因把 `frozenset` 当作可下标列表而在首店发请求前失败（`b76c1ad01`）；核对该失败执行产生的重核批次为 0，修正脚本后才进行上述成功重核。

## 尚未放行

- 12 家出库通道的**付款时间口径**未获来源认证；订单重核成功不解除此限制，不能输出 20 家混合统一 Top 10。
- 商品日聚合在 replay 前后由 160 行变为 157 行，重核后仍为 157；现有聚合检查无法还原先前 3 行的成因，需独立定位并核对金额/归属后才可宣称数据口径完全一致。
- 退款 cohort `quality_status=unknown`；售后发生来源通过不等于 cohort 质量通过。
- 用户明确选择**平台店铺可售库存**和**渠道实际在售价**。库存预警、上架价复核尚无已认证的真实来源注册：快麦 `stock.api.status.query` 的仓库/系统库存字段不自动等于店铺可售；商品档案 `priceOutput` 不等于渠道在售价。官方[商品对应关系查询](https://open.kuaimai.com/docs/api/API%E6%96%87%E6%A1%A3/%E5%95%86%E5%93%81/%E5%95%86%E5%93%81%E5%AF%B9%E5%BA%94%E5%85%B3%E7%B3%BB%E6%9F%A5%E8%AF%A2/)列出店铺/商品映射与上架时间、链接，但没有渠道价格和库存；[库存上传主动通知](https://open.kuaimai.com/docs/api/API%E5%9C%BA%E6%99%AF%E8%AF%B4%E6%98%8E/%E8%87%AA%E5%BB%BA%E5%B9%B3%E5%8F%B0/%E5%BA%93%E5%AD%98%E4%B8%8A%E4%BC%A0%E4%B8%BB%E5%8A%A8%E9%80%9A%E7%9F%A5/)仅描述**自建平台**接收 ERP 推送库存的流程，不能拿来证明现有 20 家平台店的实时可售。进一步遍历本地 `可参考/llms.txt` 的 251 项标题，并核对相关候选接口的响应字段：按商品 ID 的对应关系接口也只返回店铺/商品映射；[平台商品批次效期库存查询](https://open.kuaimai.com/__ai/markdown/api/20-API%E6%96%87%E6%A1%A3/40-%E4%BB%93%E5%82%A8/1250-%E5%95%86%E5%93%81%E6%89%B9%E6%AC%A1%E6%95%88%E6%9C%9F%E5%BA%93%E5%AD%98%E6%9F%A5%E8%AF%A2%E5%88%97%E8%A1%A8.md)虽按平台商品/SKU 返回 `totalNum`，语义是 WMS 批次效期库存，非店铺当前可售；快麦通供销小店接口的 `stock`/`levelDistributionPrice` 属于供销商品，不代表这 20 家店的渠道售价/库存。用户确认只有该索引与现有快麦 API 配置，没有额外接口资料或导出。因此继续保持 `unsupported`；这两项所选业务真值无法由目前可证明的字段推出，不以 API 返回成功替代来源认证。
- 受控 SQL 探索与隔离分析生产发布仍需 Task 11 门禁；离线测试及一次真实模型冒烟不构成生产试用放行。一日回放也未补齐 `09-18→09-23` 缺口。

本文件只保存无敏感标识的聚合结果和任务编号；不含店铺主键、订单明细、凭据或业务原始响应。
